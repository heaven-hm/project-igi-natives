# Config_SoundOptionsGetReverseStereo

Address: 0x00404EB0
Signature: int Config_SoundOptionsGetReverseStereo()
Data types: ProfileRecord, SoundOptionsRecord

## Recovered behavior

Returns 1 if stereo is reversed for the active profile, else 0.

## Evidence classification

retail-string

Use only the typed fields and prototypes proven in assets/IGINatives.json. Unknown regions remain opaque.
