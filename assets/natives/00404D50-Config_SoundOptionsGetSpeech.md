# Config_SoundOptionsGetSpeech

Address: 0x00404D50
Signature: int Config_SoundOptionsGetSpeech()
Data types: ProfileRecord, SoundOptionsRecord

## Recovered behavior

Returns 1 if speech is enabled for the active profile, else 0.

## Evidence classification

retail-string

Use only the typed fields and prototypes proven in assets/IGINatives.json. Unknown regions remain opaque.
